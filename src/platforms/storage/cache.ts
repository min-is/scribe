// Stub module for storage cache
// TODO: Remove when admin actions are cleaned up

export async function getStorageUploadUrlsNoStore(): Promise<{ url: string, uploadUrl: string }[]> {
  return [];
}
